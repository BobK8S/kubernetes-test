apiVersion: v1
kind: Pod
metadata:
  name: simple-webapp-color
  labels:
    app: web
spec:
  containers:
    - name: simple-app
      image: mmumshad/simple-webapp-color
      ports:
       - name: web
         containerPort: 80
      env:
       - name: APP_COLOR
         value: red